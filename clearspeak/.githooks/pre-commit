#!/usr/bin/env sh
# Runs lint-staged (Prettier) on staged files before every commit.
# Installed via: git config core.hooksPath clearspeak/.githooks
set -e
cd "$(git rev-parse --show-toplevel)/clearspeak"
npx lint-staged
