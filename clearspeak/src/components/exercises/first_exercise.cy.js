describe("Dyslexia PWA - Pierwsze uruchomienie i ćwiczenie",()=>{beforeEach(()=>{cy.clearLocalStorage()});it("ładuje aplikację, przechodzi przez ekran powitalny i próbuje rozwiązać pierwsze ćwiczenie",()=>{cy.visit("/");cy.contains(/EnClaro/i).should("be.visible");cy.contains(/Tylko nauka|Study only/i).click();cy.contains(/Rozpocznij|Start/i).click();cy.get("aside").should("be.visible");cy.get("section").should("be.visible");cy.contains(/Brak zadań|No tasks/i).should("not.exist");cy.contains(/Błąd formatu|Task format/i).should("not.exist");cy.get("section").within(()=>{cy.get("button").not(':contains("🎤")').not(':contains("🛑")').not(':contains("🔊")').last().click({force:true})});cy.get("body").should("not.contain","Wystąpił błąd.")})});